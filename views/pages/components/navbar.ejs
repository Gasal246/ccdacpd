<nav class="navbar navbar-expand-lg navbar-light p-3" id="navbar">
  <div class="container-fluid">
    <a class="navbar-brand" href="#"
      ><img src="/assets/images/icons/logo.png" width="110" alt=""
    /></a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavDropdown"
      aria-controls="navbarNavDropdown"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <a class="nav-link mx-2 n-btn" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link mx-2 n-btn" href="/cencus">Cencus Form</a>
        </li>
        <li class="nav-item">
          <a class="nav-link mx-2 n-btn" href="/cdata">Cencus Data</a>
        </li>
      </ul>
      <ul class="navbar-nav ms-auto d-none d-lg-inline-flex">
        <li class="nav-item dropdown">
          <a
            class="nav-link mx-2 dropdown-toggle"
            href="#"
            id="navbarDropdownMenuLink"
            role="button"
            data-bs-toggle="dropdown"
            aria-expanded="false"
          >
          <%= cookData.admin ? cookData.admin.name : 'Guest' %>
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
            <li>
              <a class="dropdown-item" id="logout-admin"
                ><i class="fa-solid fa-right-from-bracket"></i> logout</a
              >
            </li>
            <li>
              <a class="dropdown-item" href="/settings/<%= cookData.admin ? cookData.admin.id : 'Guest' %>"
                ><i class="fa-solid fa-gear"></i> settings</a
              >
            </li>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>
<div id="progress-bar" class="fixed-top"></div>

<script>
  document.getElementById("logout-admin").addEventListener("click", async (e) => {
    e.preventDefault();
    let query = await showConfirm("Are you really leaving CCDA..!?");
    if(query) {
      $.ajax({
        url: "/logout",
        method: "post",
        success: (res) => {
          if (res.success) {
            location.reload();
          }
        },
      });
    }
  });
</script>
